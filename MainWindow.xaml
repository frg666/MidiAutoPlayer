<Window x:Class="MidiAutoPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:MidiAutoPlayer"
        xmlns:view="clr-namespace:MidiAutoPlayer.View"
        mc:Ignorable="d"
        Title="MidiAutoPlayer"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <view:MidiView x:Name="MainView" />
        <view:SettingsView x:Name="SettingsView" Visibility="Collapsed" />
        
        <ui:Button x:Name="Button_Settings" 
                   Content="设置" 
                   VerticalAlignment="Top"
                   HorizontalAlignment="Right"
                   Margin="0,10,10,0"
                   Appearance="Primary"
                   Click="Button_Settings_Click" />
    </Grid>
</Window>
